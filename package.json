{
  "name": "barcode-reader-web-app",
  "version": "1.0.0",
  "description": "Barcode reader app with TailwindCSS and Flask",
  "main": "index.js",
  "scripts": {
    "build:css": "npx tailwindcss build src/tailwind.css -o static/css/tailwind.css",
    "build": "npm install && npm run build:css",
    "start": "gunicorn main:app"
  },
  "dependencies": {
    "flowbite": "^2.4.1",
    "tailwindcss": "^3.4.6"
  }
}
