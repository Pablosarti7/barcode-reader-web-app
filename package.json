{
  "name": "barcode-reader-web-app",
  "version": "1.0.0",
  "description": "Flask ingredients advisor web app with TailwindCSS for styling",
  "scripts": {
    "build:css": "npx tailwindcss -i src/tailwind.css -o static/css/tailwind.css",
    "build": "npm install && npm run build:css",
    "start": "gunicorn main:app"
  },
  "dependencies": {
    "flowbite": "^2.4.1",
    "tailwindcss": "^3.4.6"
  }
}
